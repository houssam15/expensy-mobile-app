import "package:flutter/material.dart";

class ExpensySplashRepository extends StatelessWidget {
  const ExpensySplashRepository({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
